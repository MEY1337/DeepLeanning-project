# House Prices 예측을 위한 딥러닝 모델 개발 프로젝트 최종 보고서

## 1. 프로젝트 개요

본 프로젝트는 Kaggle의 House Prices - Advanced Regression Techniques 데이터를 활용하여 주택 가격을 예측하는 딥러닝 모델을 개발하는 것을 목표로 수행하였다.

주택 가격은 주택의 면적, 품질, 건축 연도, 차고 규모 등 다양한 요인의 영향을 받기 때문에 단순한 선형 관계만으로 설명하기 어렵다. 따라서 여러 변수 간의 복잡한 관계를 학습할 수 있는 딥러닝 모델을 적용하여 보다 정확한 가격 예측을 시도하였다.

## 2. 데이터셋 소개

본 프로젝트에서는 Ames Housing Dataset을 사용하였다.

- Train 데이터 : 1,460개
- Test 데이터 : 1,459개
- 설명 변수(Feature) : 79개
- 목표 변수(Target) : SalePrice

### 주요 변수
- OverallQual : 주택 품질
- GrLivArea : 실제 거주 면적
- GarageCars : 차고 수용 차량 수
- YearBuilt : 건축 연도
- TotalBsmtSF : 지하실 면적

## 3. 데이터 전처리

- SalePrice를 목표 변수(y)로 분리
- 수치형 변수만 선택하여 사용
- 결측치를 평균값으로 대체
- StandardScaler를 이용한 정규화 수행

## 4. 탐색적 데이터 분석(EDA)

SalePrice의 분포를 분석한 결과 대부분의 주택 가격이 특정 구간에 집중되어 있었으며 일부 고가 주택이 존재하여 오른쪽으로 긴 분포를 나타내었다.

또한 상관관계 분석 결과 OverallQual, GrLivArea, GarageCars 등의 변수들이 주택 가격과 높은 양의 상관관계를 보이는 것을 확인하였다.

## 5. 딥러닝 모델 설계

### 모델 구조

- 입력층
- Dense(128, ReLU)
- Batch Normalization
- Dropout(0.2)
- Dense(64, ReLU)
- Batch Normalization
- Dropout(0.2)
- Dense(32, ReLU)
- Dense(1)

출력층은 회귀 문제이므로 활성화 함수를 사용하지 않고 하나의 연속값을 출력하도록 구성하였다.

## 6. 모델 학습 및 평가

- Optimizer : Adam
- Loss Function : MSE
- Evaluation Metric : RMSE

학습 손실과 검증 손실을 함께 관찰하여 모델의 학습 상태를 확인하였다.

## 7. 프로젝트의 한계점

- 수치형 변수만 사용
- 범주형 정보 미반영
- 과적합 가능성 존재

## 8. 개선 방향

1. 범주형 변수 인코딩 후 활용
2. Feature Engineering 수행
3. Early Stopping 적용
4. 하이퍼파라미터 최적화
5. 교차 검증 적용
6. 앙상블 기법 활용

## 9. 결론

본 프로젝트에서는 House Prices 데이터를 활용하여 주택 가격을 예측하는 딥러닝 모델을 구축하였다.

데이터 전처리, EDA, 모델 설계, 학습 및 평가 과정을 수행하면서 딥러닝 기반 회귀 모델의 동작 원리를 이해할 수 있었다.

향후에는 보다 다양한 변수와 고급 기법을 적용하여 예측 성능을 향상시키고 실무 수준의 모델 개발에 도전하고자 한다.
